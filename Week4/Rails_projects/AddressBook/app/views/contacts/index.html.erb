<!-- app/views/projects/index/html.erb -->


<h2> All the contacts </h2>

<ul>
	<% @contacts.each do |contact| %>
		<li>
			<%= link_to contact.name, contact_path(contact.id)%>
		</li>
	<% end %>
</ul>